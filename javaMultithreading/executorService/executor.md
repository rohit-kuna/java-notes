- submit()
    - when expecting a result in form of Future<V>
    - can pass runnable, callable
- execute()
    - when we want to just execute a runnable

NEW → COMPLETING → NORMAL (done)
                 → EXCEPTIONAL
                 → CANCELLED
